import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "机构会员申请｜国际道教与文化协会 ITCA"
};

export default function OrganizationApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
