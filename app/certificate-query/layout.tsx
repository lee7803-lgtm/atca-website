import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "证书查询｜国际道教与文化协会 ITCA"
};

export default function CertificateQueryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
