import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "申请进度 / 申请结果查询｜国际道教与文化协会 ITCA"
};

export default function ApplicationQueryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
