import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "证书核验｜国际道教与文化协会 ITCA"
};

export default function VerifyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
