# AGENTS.md

## Fixed Project Rules

- 不自动 push。
- 不自动发布 production。
- 不自动执行 SQL。
- 不直接修改生产数据库。
- V2.0 采用导航升级版方案。
- V2.0 视觉延续 V1.3，不做视觉系统重构。
- HTML 原型只作为信息架构参考，不作为最终视觉代码。
- 必须保留 V1.3 申请、审核、支付、证书、核验、通知、后台基础资料管理闭环。
- 本轮 V2.0 后续目标包含用户系统和后台 RBAC 权限体系。
- 如果需要用户系统或权限相关数据库结构，只能新增 SQL 草案文件，不得执行。
- 所有认证、道医中医、易学与东方认知、数据中心内容必须有边界说明。
- 完成后必须汇报新增 SQL、是否执行 SQL、是否修改数据库、`git diff --check`、`git status`、commit id、是否 push、是否发布。

